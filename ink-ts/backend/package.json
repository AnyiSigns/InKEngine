{
  "name": "@ink-ts/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "L4 宿主装配层：服务键注册/inject/effect dispose；存储与宿主级领域服务（机制语义在 engine）",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  }
}
