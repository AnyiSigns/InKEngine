# InKling 会话 = Agent 图生命周期：组装时点 / 架构模板 / 展开三通道 / 演化双入口 设计稿

> 🔄 **三次收敛（2026-09-09 最小原语模型）＝当前执行主线，权威稿见
> `agent_execution_design.md`**：执行 = **水流 × 作用域 × 通道**；组装/每会话
> 建图/图生命周期**废除**；图降为**投影**（不限制执行）；择优与进化的对象 =
> **组织资产**（委托/并行/作用域分派/通道条件）。本文 §九 保留该收敛的决策与
> 迁移记录；**执行语义冲突处以 `agent_execution_design.md` 为准**。
>
> 状态：**设计定稿（2026-09-09 对话收敛）**。
> ⚠️ **二次修订（2026-09-09 组装哲学收敛）**：新增 §八——图=异构编排
> （多 kind 节点 × 多语义边）、组装协议（内核 + 类型化端口/通道 + 语义脚手架）、
> 模板=择优结晶、生成—评估—选择环、执行体真分化、节点边界质量闸；对 §三
> P5 队列的计划性修订以 §八 为准。前置依赖 =
> `graph_type_system_design.md` P4 队列完成（P4-A/P4-B 已实现，剩余
> agent 工具包面/auto 独立计数等后续批次）。本稿 P5 待前置完成后排期实现。
> 实现以本文件为准，各阶段回填文末「落地状态」栏；实现与设计差异须同步
> 本文件（维护纪律：设计稿定稿带「落地状态」栏，参照
> `graph_type_system_design.md` 与 `inkling/docs/multi_agent_design.md`）。
> 范围：ink-ts 全链路——engine 组装语义（分诊门/架构模板/槽位填充/会话结构
> 档案）+ host 命令面 + UI（演化沉淀时间线双轨）。
> 定位：不改产品定位语（manifest.json 单一事实源）；本文只收敛机制语义。
> 与旧稿关系：**深化并局部替代** `graph_type_system_design.md` 的回合语义
> 表述，继承其 P1–P4 全部机制件（见 §七 继承与差异表）。

---

## 〇、缘起与反思结论（本稿要修正的三个认知）

1. **组装不是每轮的动词**。P4 会话级骨架定案后，回合沿骨架推进是常态；
   旧稿残留的「按轮观察图/按轮组装」表述与骨架模式冲突。本稿定案：组装是
   **离散结构事件**，只发生在两个时点（首个真目标 / 演化采纳后），见 §一.2。
2. **「每轮重组装 = 多样化」与结构学习互斥**。结构每轮都变，回合结果就无法
   归因到结构——跨会话择优就成了空话。**归因优先**，所以「一个会话 = 一个
   图生命周期」不是妥协，是结构学习成立的前提。多样化的主战场是**跨会话**，
   不是回合内。
3. **现状 ≠ 愿景要分账**。现状引擎组装器（`_forward_search`）只产**线性链**，
   无分支/回环/层级搜索空间；监督/工作流/嵌套拓扑不存在于生成侧。本稿不承诺
   「拓扑搜索涌现」，改走**受审模板 + 槽位填充**（§一.5）——模板是资产，
   组装只做实例级填充。

另：本轮对话澄清的词义冲突已收敛为术语表（§一.9），后续文档/代码注释一律
按表用语，避免「组装/推演/展开/进化/孵化」一词多义复发。

---

## 一、大设计（语义收敛定稿）

### 1.1 会话 = 一个 agent 实例的图生命周期

**核心对象不是「图」也不是「回合」，是会话**：一个 agent 实例从建图到
checkpoint 收尾的完整生命周期。图是该生命周期的结构载体。

- 身份边界恒一：会话期间事件/记忆/审计/产物统一归该 agent；
- 无论内部是线性、工作流还是监督团队，对会话外只呈现一个 agent；
- agent = 递归展开：图里的 `kind=agent` 结点展开又是子图（`§2.3 已有`），
  递归无上限但受深度护栏约束；
- 会话尺度数据 = 骨架/游标/当前执行态（随 thread checkpoint 恢复）；
  演化产物永不「专属本会话」（进化即留存不变量，继承旧稿）。

### 1.2 组装 = 低频离散结构事件（替代「按轮组装」）

图结构只在两个时点被构造/修改：

| 时点 | 动作 | 说明 |
|---|---|---|
| **t0 首个真目标出现** | 架构选择 + 模板槽位填充 + 校验 → 会话图（骨架）落地 | 问候/闲聊等无任务回合**不触发**（分诊门，§一.3） |
| **tN 演化采纳后** | 局部改骨架（声明式）或重组装（新资产入池） | 采纳 = 受控事件（审批/补丁链），非默认节拍；自续跑 carry 此语义 |

其余回合一律**沿图推进**：`active_target` 游标移动、router 分支、条件边
选向、LLM 判定执行——图结构冻结，只有行为不确定。

### 1.3 分诊门（新首步）

组装触发条件不是「用户发了消息」，而是「出现了值得一张任务图的目标」：

- **无目标回合**（你好/闲聊/澄清）：走最小形态——单终态结点直接回复，
  零架构决策、零模板填充、不建立任务骨架；
- **有目标回合**：进入架构决策（§1.4）→ 建图。
- 判定分级（由轻到重，取先命中）：静态启发（输入空 schema/问候特征）
  → 可配置 planner 判定（复用 llm 实例，成本档位可关）。
- 与现状兼容：当前引擎对空目标已有回落语义
  （`assembler.ts` goal 为空返回 fallback_reason）——分诊门是把该语义
  提升为**显式首步**，并把「建图时机」从「会话第一条消息」改为
  「首个真目标」（问候开局时二者不同）。

### 1.4 架构 = agent 内部组织模式

架构不是第四类构件，是**会话图的组织形态**，全部在 agent 身份边界之内。

**模式集 = 三种组织模式 + 一个递归轴**（不引入第四种平级模式）：

| 模式 | 判别特征（拓扑） | 触发线索（目标分析输入） |
|---|---|---|
| 线性 | 无分支无环链 | 单意图、无质量门、无并行需求 |
| 工作流 | router 条件分支 / 门回环（检查→返工） | 多子任务需分派、需确定性流程门 |
| 分级监督 | 监督者 + 多 worker + 审阅回边 | 质量关键、可并行分解、需终审 |
| （嵌套 = 递归轴） | 任意模式的结点是 `agent` 子图 | worker 自身复杂需再分解；由递归提供，不是并列模式 |

**选择时机**：t0 建图时（会话级）；每个 `agent` 结点展开时**可再选一次**
（与「召唤 = 挂 agent 结点并展开内部回路」同构，选择随递归下钻）。

**选择权限分层**（低到高，高者覆盖低者）：

1. 算法启发（默认）：静态特征→触发线索表→模式；
2. agent 声明：声明式改骨架时携带 `structure` 意图，经 `validate_skeleton`
   校验后生效；
3. 用户显式：最高优先，会话/线程级覆盖。

**降级链（护栏）**：所选模式的槽位无法从池满足 → 不硬拼，沿
「分级监督 → 工作流 → 线性 → 单终态兜底」逐级降，降级原因入审计。
架构是意图不是承诺。

### 1.5 架构模板机制（P5 核心新机制）

监督/工作流等拓扑**不做搜索涌现**，做**受审模板**：

- **模板 = 资产**（数据），字段含：`structure_type`、槽位定义（每个槽位的
  契约要求/允许多实例/worker 数范围）、图不变量（回边要求/终态可达/
  监督者必达终审）、校验规则、降级链、`origin`（seed/host/agent/governance）、
  `status/version`——**形状对齐池治理登记行与实体目录**；
- 模板只定**结构空位**，执行体仍只能来自池（宿主/内置/包绑定）——不引入
  运行时任意代码，继承旧稿不变量 7；
- **来源**：出厂三模板（线性/工作流/分级监督最小可执行版，含验证用链，
  参照 P4.2a 出厂池种子扩充的先例）；agent/宿主/包可注册新模板，走
  审批 + 补丁链（复用「新类型/新 agent 诞生边界」的闸门语义）；
- **槽位填充**：模板实例化时，组装器在槽位内做契约匹配（复用现有
  contract pool 视图 + beam，但搜索被模板约束到槽位，而非全域链搜索）；
  同一 kind 多实例分化（如 planner/reviewer 写不同输出键）是槽位可满足性
  的前提——池里无分化实例 = 该槽位不可满足 = 触发降级链。
- 模板变化本身可随会话结构档案的跨会话择优结果演化（§1.7）。

### 1.6 展开三通道 = 同一子作用域基座的三个提交契约

代码事实：`spawn`（`run_spawned`）、决策点推演（`run_simulated`）、会话试跑
（`fork_trial`）共用同一实例化基座（子引擎 + 独立 checkpoint 子链 + 事件汇
入父链 + 成本护栏），**唯一差异是结果提交契约与作用域尺度**。本稿不新增
机制件，只做术语收口与角色升级：

| 通道 | 提交契约 | 作用域 | 用途 |
|---|---|---|---|
| spawn | **全部回流** | 图中一段子图 | 并行干活，子任务结果全要 |
| 决策点推演 | **择优回流** | 决策点候选分支 | 不确定走向先跑后选，落选保轨迹 |
| fork_trial | **不回流（只给决策）** | 整个会话（骨架+消息基线） | 会话级假设验证，主线不动 |

统一伞词：**展开**。三问定通道：结果要不要回来 / 边界多大 / 谁发起。

**fork_trial 角色升级（本稿新增语义）**：作为「演化采纳前验证闸」——
agent 自修改提案落地主线**之前**，先 fork 试跑一轮；试跑线程的胜负
**不进主线证据**（归因保险，与探索预算「试跑样本不污染统计」同构）。
试跑采纳后才经重组装上主线。

### 1.7 演化双入口 = 同一资产层同一条受控通道

| 入口 | 驱动 | 路径 | 示例落点 |
|---|---|---|---|
| 进化（有意） | agent 决策 | 提案 → 审批 → 补丁链 → 采纳后（重组装/自动续跑）生效 | self_tools / apply_patch |
| 孵化（无意） | 回合事件统计累积 | 信号 → 孵化缓冲 → 变异/结晶 → 受控落资产 | entity_evolution / 知识孵化 / skill_crystal / growth / tuning |

- 两条入口**不各建一套池**：变异与补丁走同一受控通道（GuardedStorage /
  EvolutionWriter / 补丁链 + 审计），落同一资产层；下一轮组装两种变更都读
  得到——「会话内下一轮生效 / 跨会话生效」对二者同样成立；
- 「进化即留存」覆盖**双侧**：孵化产物同样不许只活在会话里；
- 结构变更（新架构模板/骨架形态变化）属于进化侧，须审批；孵化侧只动
  池内实例先验/技能/实体 meta，不动结构模板形态。

### 1.8 归因与跨会话择优 = 结构学习的第一性问题

会话 = 一个结构 → N 个推进回合结果的**对照单元**：

1. 推进回合证据（节点成败/边证据）归本会话图（现状已如此）；
2. 会话收尾 → 沉淀**会话结构档案**：`structure_type + 组合指纹（结点/边实例
   清单）+ 结果统计（成功/失败/轮次/预算消耗）`——随 checkpoint/records 落库；
3. 跨会话在**结构档案**上择优：同 type 不同组合的档案互为对照 → 模板先验
   更新/模板废弃/槽位分化建议 → 经受控通道反馈池与模板目录。

这给「组装 = 不确定 = 多样化」以正确落点：**多样性发生在跨会话的建图点**
（每次建图是一次结构实验），回合内不再乱组。

### 1.9 术语收敛表（文档/代码注释统一用语）

| 词 | 指代 | 不指代 |
|---|---|---|
| 装配 | 引擎自身启动（Runtime 装配 ①–⑰） | 图结构构造 |
| 组装 | 图结构构造（t0 / 演化采纳后） | 引擎启动、agent 结点展开 |
| 推进 | 回合沿会话图执行（常态） | fork 试跑 |
| 展开 | 子作用域执行总称（spawn/推演/fork_trial） | 组装 |
| 推演 | 决策点 run_simulated（择优试跑） | 正常回合 |
| 会话试跑 | fork_trial | spawn |
| 进化 | 有意自修改（审批后落资产） | 统计变异 |
| 孵化 | 无意统计变异/结晶 | 有意自修改 |
| 演化 | 进化+孵化落资产的总称 | — |

---

## 二、关键语义裁定

1. **建图时刻**：首个真目标出现时建图；问候开局 = 先走最小形态，目标出现
   才升级建图（骨架空/不满足的回落语义继承 P4）。
2. **结构冻结是特性**：会话图生命中期不重建；结构变更只经演化采纳点。
3. **架构选择落点**：决策结果写入会话骨架（新增可选字段 `structure`），
   随 thread state 持久、可审计、可被声明式修改覆盖。
4. **模板槽位可满足性 = 池实例分化度**：同类多实例（不同用途/不同契约输出
   键）决定复杂架构能否成立；池无分化 = 降级，不臆造。
5. **试跑与证据隔离**：spawn/推演/fork_trial 分支与试跑线程的胜负不进主线
   证据与指纹缓存（继承 P4.1 兜底零强化/试用样本不污染统计的同一原则）。
6. **作用域模型覆盖（待实施缺口）**：协作者/agent 结点的 `model` 引用要
   在展开时作用于子作用域内 llm 调用——现状 llm seam 为宿主注入单例，需
   在展开器加 per-scope override（P5-E）。
7. **协作者定位**：实体目录 = 数据真源（persona/model/role 跨会话持久）；
   图内每次出现 = 挂 `agent` 结点实例化；运行时动态临时协作者走 spawn 通道。

---

## 三、落地蓝图（P5 队列，待 `graph_type_system_design.md` P4 收尾后排期）

依赖总览：P5-A/D 依赖 P1–P4（登记行 kind/flags、骨架、validate_skeleton、
auto 续跑）；P5-C/E/F 依赖 P5-A/D 语义；P5-G/H 依赖前序稳定后置。每阶段独立
验收、独立 commit/PR。

### P5-A —— 分诊门 + 组装触发语义收敛（engine core）

- `_runtime_rounds`/组装入口前显式首步：回合输入 → 分诊（无任务目标 = 最小
  形态单终态回复；有目标 = 建图/沿图推进）；把「首个真目标建图」与
  「会话骨架缺失走组装建立」合并为同一语义。
- 改动面：runtime rounds + assemble seam + 相关测试。
- 验收：`vitest run --root engine`、`tsc -p engine/tsconfig.json`；新增测试：
  问候首条不建任务骨架、目标出现升级建图、无目标回合走单终态。

### P5-B —— 架构模板数据面 + 模板目录（engine core 数据面 + 受控注册）

- 模板类型/登记（对齐 node_registry 形态）+ 出厂三模板种子（线性/工作流/
  分级监督最小可执行版，含验证用链——参照 P4.2a 先例）；模板走审批 + 补丁
  链注册，注册行入治理登记（继承 pool_governance 语义）。
- gated docs 同步 + 架构门禁扫描。
- 验收：引擎单测 + `tsx gate/src/check.ts`。

### P5-C —— 架构选择决策器（分层：算法启发默认 / agent 声明覆盖 / 用户显式）

- 算法启发 = 静态特征 → §1.4 触发线索表（不引入必选 llm 前置步骤）；
- agent 声明 = `skeleton.edit` 携带 `structure`，经 `validate_skeleton` 挂载；
- 降级链实现：槽位不可满足 → 逐级降 → 审计记录降级原因。
- 验收：架构选择确定性测试（同输入同选择）、声明覆盖、降级链逐级断言。

### P5-D —— 槽位填充组装器改造（engine path_assembler）

- 模板实例化入口：把 `_forward_search` 约束到模板槽位（槽位内契约匹配 +
  beam），保留线性链原路径作为「线性模板」特例（向后兼容，旧图 digest 不漂移）；
- 分支/回环图合法性校验按架构规则（复用 executor 已支持的 conditional/loop
  执行语义——P4.2a-2——生成侧补齐）。
- 验收：线性回归（既有确定性断言不变）+ 工作流/监督模板最小实例化 + 非法
  槽位降级。

### P5-E —— 作用域模型/persona 覆盖（展开器 seam override）

- `agent` 结点/spawn 实例展开时，子作用域内 llm seam 按结点 config 的
  `model` 引用覆盖（null = 继承会话默认）；协作者目录 model 字段就此生效。
- 改动面：executor 实例引擎构造 seam、node seams 注入点。
- 验收：per-scope 覆盖单测 + host 集成（不同 model 引用落到不同 adapter 配置）。

### P5-F —— 采纳前验证标准化（fork_trial 接入自修改工具面）

- 自修改/进化提案 →（可选）`rounds.fork_trial` 试跑 → 采纳才重组装上主线；
  试跑结果不入主线证据/指纹（归因保险）。
- 验收：host 集成测试——提案 + 试跑 + 采纳链路、试跑失败不采纳、证据隔离断言。

### P5-G —— 会话结构档案 + 跨会话择优（engine 沉淀侧）

- 会话收尾沉淀结构档案（structure_type + 组合指纹 + 结果统计）；跨会话在档案
  上择优 → 反馈模板目录先验/废弃建议（经受控通道）。
- 验收：档案字段往返、同结构多会话对照择优、择优结果入模板目录先验。

### P5-H —— 演化沉淀时间线双轨 UI（承接 evolution_feed 职责）

- 孵化 + 补丁双轨合一动态时间线 + 协作项目录只读；等 P3 状态页稳定 + 前序
  P5 沉淀数据形态定型后实施。
- 验收：插件单测 + `verify_unload` 孤儿扫描 + `sync_plugin_manifest` check。

---

## 四、兼容约束与不变量

1. 线性模板 = 既有链式组装的同义封装，序列化/digest 不漂移（回填不外溢）；
2. 骨架扩展字段一律可选，旧 checkpoint 反序列化回落旧语义；
3. 模板只定结构空位，执行体只能来自池/宿主/包（运行时无任意代码）；
4. 试跑/分支/推演信号不进主线证据与指纹缓存；
5. 会话图生命中期结构冻结；结构变更只经演化采纳点（受控）；
6. 架构不可满足走降级链，不臆造图（诚实空态原则继承）；
7. 术语按 §1.9 表，文档与代码注释禁旧歧义用法。

---

## 五、口径决议记录

1. 会话 = 一个 agent 实例的图生命周期；全部架构形态在 agent 身份边界之内。
2. 组装是离散结构事件（首个真目标 / 演化采纳后），非每轮动词；常态 = 沿图推进。
3. 问候/闲聊等无目标回合走最小形态，不触发架构组装（分诊门）。
4. 架构模式集 = {线性 / 工作流 / 分级监督} + 递归轴（嵌套非并列模式）；
   每个 agent 结点展开点可再选一次。
5. 架构选择权限：算法启发（默认）< agent 声明 < 用户显式；选择结果入骨架
   持久、可审计、可覆盖。
6. 监督/工作流拓扑做受审模板 + 槽位填充，不做搜索涌现；模板 = 资产走
   审批 + 补丁链；槽位不可满足走「监督→工作流→线性→单终态」降级链。
7. spawn（全回流）/ 推演（择优回流）/ fork_trial（会话假设不回流）= 同一
   子作用域基座的三个提交契约，统一伞词「展开」；不新增机制件。
8. fork_trial 升级为「演化采纳前验证闸」；试跑胜负不进主线证据（归因保险）。
9. 进化（有意）与孵化（无意）双入口落同一资产层同一条受控通道；「进化即
   留存」覆盖双侧。
10. 归因第一性：会话 = 结构对照单元；会话收尾沉淀结构档案；跨会话在档案上
    择优反馈模板目录——多样化主战场是跨会话建图点。
11. 术语收敛按 §1.9 表执行。

---

## 六、落地状态

| 设计项 | 状态 | 备注 |
|---|---|---|
| §一 大设计语义收敛 | 定稿（2026-09-09） | 待 `graph_type_system_design.md` P4 收尾后进入实现 |
| §三 P5-A 分诊门 + 组装时点 | 待排期 | 依赖 P4 收尾 |
| §三 P5-B 架构模板数据面 + 目录 | 待排期 | 出厂三模板种子待实现 |
| §三 P5-C 架构选择决策器 | 待排期 | 分层 + 降级链 |
| §三 P5-D 槽位填充组装器改造 | 待排期 | 线性原路径向后兼容 |
| §三 P5-E 作用域模型/persona 覆盖 | 部分实现 | 引擎 core 先行（2026-09-09，批4：#9，见 `graph_type_system_design.md` P4.2a-4）：agent 结点展开器 per-scope llm override（resolve_scope_llm seam / recipe.scope_model_llm 装配接线位；model:null = 继承父作用域/会话默认）；persona 作 scope system 自定义层并入（boot 基线不绕过）；spawn 实例展开的模型覆盖、宿主集成接线、模板级嵌套 scope 语义仍待排期 |
| §三 P5-F 采纳前验证标准化 | 待排期 | fork_trial 接入自修改工具面 |
| §三 P5-G 会话结构档案 + 跨会话择优 | 待排期 | 归因承载体 |
| §三 P5-H 演化沉淀双轨 UI | 待排期 | 依赖前序数据形态定型 |
| §八 二轮修订（8.2–8.6） | 设计定稿 | 计划性；P5-I/J/K/L 新增阶段并入 P5 队列，见 §八 8.6 |
| §九 最小原语模型（决策记录） | 设计定稿（2026-09-09） | 权威执行稿 = `agent_execution_design.md`；组装类语义冲突以执行稿为准 |

---

## 七、与 `graph_type_system_design.md` 的继承与差异

| 旧稿条款 | 关系 | 说明 |
|---|---|---|
| P1–P3（类型建模/废除 base/状态页） | 继承 | 机制件不变 |
| P4（会话级骨架/自进化续跑/自续护栏） | 继承 | 本稿的 t0/tN 时点模型以其为底座 |
| 决议 9「默认走会话级骨架」 | 深化 | 升级为「会话 = 图生命周期」，建图时机 = 首个真目标 |
| 旧「每轮观察图/按轮组装」表述 | **替代** | 组装为离散结构事件；回合内图冻结 |
| P4.1 探索预算（兜底零强化/试用/反垄断） | 继承 | 与试跑证据隔离同原则；跨会话择优（P5-G）为其结构侧延伸 |
| P4.2 池种子多样化 | 继承 | 复杂架构的槽位可满足性依赖实例分化（P5-B/D） |
| 决议 11 类型/agent 诞生边界 | 扩展 | 架构模板走同一审批 + 补丁链闸门 |
| 决议 13「可分 = 推演 fork」 | 深化 | 三通道统一 + fork_trial 采纳前验证闸（P5-F） |
| 实体 → agent 结点（§2.4） | 继承 | 协作者 model 生效缺口列入 P5-E |
| 决议 15/16（P4.2a/b） | 继承 | llm 实例分化 + boot 基线语义不变 |

---

## 八、二轮设计修订（2026-09-09 组装哲学收敛）

> 状态：**修订定稿（计划性）**。本节是 2026-09-09 二次对话（会话图组装产物
> "强扭 / 不结构化 / 各干各的" 诊断）的判断记录点与计划性修订；实现并入下文
> 标注的对应 P5 阶段，落地状态随各阶段回填 §六 与本表。与 §0 认知 1–3 的
> 关系：继承不推翻；本节把它们往「组装协议、模板定位、好图来源」再推一层。

### 8.1 诊断收敛（新增认知 4–7，§0 认知 1–3 的延展）

| # | 认知 |
|---|---|
| 4 | 图是**异构**的：多 kind 节点（llm/tool/router/agent/perception/…）× 多语义边（数据/条件/回环/层级）。LLM 决策链只是其中一种退化形态。生成/校验模型若停留在"同质线性字段链"（现状 `_forward_search`/`validate_chain`），router 分支、tool 消费、agent 展开在结构上无表达——"不结构化"的一半根因在此 |
| 5 | "分化实例"不等于分工：executor 同指 `engine:llm_decider`、仅提示词/config 不同的 `llm_planner/reviewer/main` 是"一个 agent 的三张面具"；真角色分化要求执行语义不同（内部循环/自校验/工具集/失败模式），或由 agent 实体（子图）承载角色 |
| 6 | 语义咬合不能靠"组装时保证"（搜索与模板都无法先验判定语义兼容）：改为「节点边界适配兜**可用**（入边规整 + 出边产物校验/修复）+ 择优淘汰兜**好用**」，发生器因此才敢探索新结构 |
| 7 | cordis/dsh 只借鉴不套用：它们组合的是"能力平台"（内核 + 依赖/事件挂载 + 固定 loop + 整 agent 委托），不承担"任务→结构映射"；该映射是本项目独有的难题，须由 8.3 的生成—评估—选择环承担 |

**修订关系说明**：§0 认知 1–2 与 §五 决议 1–5、7–11 继承不变；**§0 认知 3 与
§五 决议 6 被本修订推进**——"不做拓扑搜索涌现、受审模板 + 槽位填充"保留为
**利用基线**，同时开放"类型合法结构空间的有界生成"（P5-K）为**探索通道**：
模板是择优结晶的缓存与初始种群，发生器在类型合法 + 组装校验约束内探索新
结构，非认知 3 所拒绝的自由涌现（类型闸把"能运行但不合法"的结构挡在门外）。
§一.2（t0 建图动作）、§一.5（模板槽位填充）、§二.4（槽位可满足性）、§三
P5-B/D/G 的既有表述，均按本节修订口径理解。

### 8.2 组装协议：内核 + 类型化端口/通道 + 语义脚手架（新数据面）

> 取代"裸键 read_fields + 任意顺序边"作为组装契约的语义基础；§1.5 槽位契约
> 与 P4.2a-3 实例分化在此协议上定义。动机：节点/边实例持续增长时，组装
> 正确性只能由**类型化接口**保证，不能靠"中心点语义百科"（语义百科随实例
> 增长不可维持，类型系统可以）。

- **节点 = { kind 内核 + 端口面 + 语义脚手架 }**
  - **kind 内核（固定）**：每 kind 一个固定执行协议。llm = 读输入端口包 → LLM/工具回合 → 按输出端口声明写产物；router = 读状态 → 判定 → 置路由游标；tool = 消费 pending → 执行 → 产出；agent = 展开实体子图并按回流契约提交。内核不随实例变化。
  - **端口面（类型化）**：`in_ports[]/out_ports[]`，每端口 = {名称, 契约类型, 形态 schema}。端口是 state 的**命名视图声明**（out_port→state.plan 等）——共享 state 原地升级为端口语义，执行引擎兼容演进，不另起运行时通道。
  - **语义脚手架（可变，形态受控）**：实例级可变语义（system_prompt/read_fields/output_field/阈值/…）。脚手架**形态**类型化且受控注册：内容可变、形态种类有限（防类型爆炸 = 字符串键灾难重演）。
- **边 = { 通道内核 + 通道类型 + 两端端口绑定 }**：数据通道（搬运/规整）、条件通道（按游标判路）、回环通道（重入/游标）、层级通道（agent 子图展开）。边不再是无类型顺序连接。
- **组装校验（validate 面）**：端口类型兼容 + kind×通道组合合法 + **消费性**（中间产出必被消费；`[planner, decider]` 类直接出局）+ 终态可达。
- **端口/通道类型目录**受控注册（复用 node_registry/pool_governance 登记 + 补丁链）；新类型 = 新资产走审批。

### 8.3 模板重新定位 + 生成—评估—选择环（修订 §1.5 / §1.8）

> §1.5 把模板作为复杂拓扑的受审来源，方向保留；但**模板不能是好图的唯一
> 来源**——纯模板 = 利用陷阱：结构沿模板加深、多样性死亡；纯搜索 = 不可控
> 强扭。二者不是二选一，是同一闭环上的利用与探索。好图由三层环产出：

- **发生器（探索）**：在类型合法结构空间生成候选图（含分叉/回环/层级，非仅链）；模板 = 初始种群与变异偏向，不是结果集合；发生器输出经 8.2 组装校验才可入选。
- **评估器（试错）**：fork_trial/spawn 低成本试跑 + 结果归因到结构（会话 = 结构对照单元，§1.8）。评估够便宜、归因够清楚，是多样性能否成立的前提。
- **选择器与记忆（收敛）**：会话结构档案择优 → 胜者**结晶**为模板/先验（加速后续选择）、败者**降权废弃**；模板目录自身演化（有结晶有废弃），不单向加深。
- 语义由边界适配（8.5）兜底后，发生器可大胆尝试新结构；坏结构被择优淘汰，好结构结晶为记忆——**多样性 = 发生器覆盖的结构空间宽度，收敛 = 择优与记忆**，两压力对抗，种群不向模板单向聚拢。

### 8.4 执行体真分化（修订 P4.2a-3 的"分化"语义）

- "实例分化"验收标准从"config 字段不同"升级为"**执行语义不同**"：共享同一执行体（如 engine:llm_decider）、仅提示词不同的实例不算真角色分工，属提示词换皮。
- 真角色两条路径：① 独立执行体/子循环（各自自校验、重试、失败模式、工具集）；② 由 agent 实体（子图，persona/scope/model 自洽）承载角色，主图只挂 agent 结点。跨角色协同主载体 = agent 递归委托（§1.1 递归轴）；llm 链只做 agent 内部实现细节或冷启动兜底。

### 8.5 节点/边边界质量闸（新机制，按 kind × 通道注册）

> 借鉴 dsh 工具调用三道瀑布（pre-execute guard → approval → post-execute hook）
> 转译到图边界——让"上游不守约"从全线破功变为"边界必守约"，质量闸不写死在
> 节点提示词：

- **入边规整**：上游产物按本节点输入 schema 规整为期望形态（纯文本 → 结构化）再进提示；
- **出边产物校验 + 修复/重试/降级**：产出不符合声明输出 schema → 修复或重试，仍不合 → 判失败走降级链；
- **kind 特定闸**：router 分支 key 指向存在的条件边；tool 权限与结果可消费性；agent 回流契约；llm 产出 schema。

### 8.6 计划变更（并入/修订 §三 P5 队列；原阶段不删，只标注修订）

| 阶段 | 变更 | 内容 / 验收要点 |
|---|---|---|
| P5-B 模板目录 | 修订 | 模板 = 择优**结晶**产物：数据面带 origin/被选次数/废弃位；目录支持结晶与废弃；出厂三模板仅作种子/初始种群 |
| P5-D 槽位填充 | 修订 | 从"槽位内字段链 beam"升级为 8.2 组装校验（端口类型兼容 + 消费性 + kind×通道合法）；线性链保留为线性模板特例（digest 不漂移） |
| P5-G 结构档案 | 升级为闭环核心 | 档案带结构组合指纹（kind + 通道）+ 结果统计 + **结构多样性观测键**；跨会话择优直接驱动模板结晶/废弃（8.3） |
| P5-I（新增） | 组装协议数据面 | 节点注册端口面 + 脚手架类型声明、边注册通道类型；NodeContract/field_io/instance_contract 派生升级到端口/形态 schema；组装校验与剪枝单测（`[planner, decider]` 出局断言） |
| P5-J（新增） | 边界质量闸 | 按 kind × 通道注册的入边规整与出边产物校验/修复 seam（引擎 seam + 单测：纯文本入 → 结构化可用；非法产物 → 修复/降级） |
| P5-K（新增） | 结构发生器 | 类型合法结构空间生成/变异算子（分叉/回环/层级），模板为偏向；输出经 P5-I 校验方可入选 |
| P5-L（新增） | 执行体真分化 | llm 实例分化从 config 层验收升级到执行体层；不满足者改由 agent 实体承载角色 |

**排期建议**（并入 §三 依赖总览）：P5-I（组装协议数据面）先行，P5-D 修订与
P5-K（结构发生器）的校验面依赖它；P5-J（边界质量闸）可在 P5-I 后独立推进；
P5-L 相对独立；P5-B 模板结晶化与 P5-G 结构档案依赖前序稳定（继承 §三
"P5-G/H 依赖前序稳定后置"）。

统一验收：引擎单测 + `tsc -p engine/tsconfig.json` + `tsx gate/src/check.ts`（涉及 gated docs 时架构门禁随跑）。

### 8.7 修订口径（并入 §五，决议编号续 12）

12. 图是异构编排（多 kind × 多语义边）；LLM 决策链是退化特例。
13. 组装正确性靠类型化端口/通道 + 受控类型目录，不靠中心点语义理解。
14. 模板是择优结晶的缓存而非好图源头；好图产自生成—评估—选择环。
15. 节点"分化"须到执行语义层；跨角色协同主载体为 agent 递归委托。
16. 节点边界质量闸（按 kind 注册）兜"可用"；结构档案择优兜"好用"。

### 8.8 落地状态（本节）

| 项 | 状态 | 备注 |
|---|---|---|
| 8.1 诊断收敛 | 定稿（2026-09-09） | 认知 4–7 |
| 8.2 组装协议三层模型 | 设计定稿 | 计划性；数据面由 P5-I 承载 |
| 8.3 生成—评估—选择环 | 设计定稿 | 修订 P5-B/D/G；发生器由 P5-K 承载 |
| 8.4 执行体真分化 | 设计定稿 | P5-L |
| 8.5 边界质量闸 | 设计定稿 | P5-J |
| 8.6 计划变更 | 待排期 | 并入 P5 队列后回填 |

---

## 九、最小原语模型：水流 × 作用域 × 通道（三次收敛：决策与迁移记录）

> 状态：**决策与迁移记录（2026-09-09）**。本节记录组装语义废除的收敛过程与新旧
> 映射；**权威执行稿 = `agent_execution_design.md`**，冲突以该稿为准。

> 状态：**决策与迁移记录（2026-09-09）**。本节记录对 §0–§八 的收敛裁决：
> 执行 = 水流 × 作用域 × 通道；组装废除、图降为投影、择优/进化的对象 =
> 组织资产。**权威执行稿 = `agent_execution_design.md`**。落地状态见 9.9。

### 9.0 收敛路径与判定（为什么组装不成立）

现象与推演链（2026-09-09 实验与对话共同收敛）：

1. 组装产物"强扭"：三 LLM 节点各答一遍、中间产物空转/元混淆、计划型任务图不随任务
   变化——算法字段搜索只能保证"能运行"，不保证"结构成立/好用"。
2. 模板化同样不成立：模板若成为好图的唯一来源 = 利用陷阱，结构沿模板加深、多样性死亡。
3. cordis/dsh 参照：内核 + 能力装配 + 固定 loop + 整 agent 委托可行；但它们组合的是
   "能力平台"，从不承担"任务 → 结构映射"——该映射本就不该由运行时推断。
4. 形态迭代：组装链 → 受审模板 → 网/河流 → 单图常驻 → per-agent 多图(否) →
   地下河(跨作用域转场) → "为何要用图限制执行" → **执行去图化，组织择优保留**。
5. 实验（`live/experiment_river`）佐证：免费模型下"主执行体自治 + 加工即判定 + 跳海"，
   寒暄一次调用收敛、复杂任务按需伸出支流再汇流，成本随自治决策而非护栏控制。

**判定**：既然图是持续保留的资产而执行又是自治的，"每次/每会话拼一张执行图"就没有
存在意义。执行不需要静态拓扑；需要的是三个最小原语 + 一张投影出来的图。

### 9.1 三个原语

| 原语 | 定义 | 内容 |
|---|---|---|
| **水流 Flow** | 执行单位（一次会话/子任务实例） | 上下文 + 任务 + 产物(载荷) + 作用域 + 轨迹（经过的 scope×channel 序列）；可派生子水流 |
| **作用域 Scope** | 执行身份定义（agent/协作者/子代理） | persona / model / 可用能力 / 规则与权限 / 成本档 / 输入输出契约；目录资产，受控注册（审批/补丁链） |
| **通道 Channel** | 跨作用域/水流间的转场算子 | 形态：委托(1→1)、fan-out(1→N)、fan-in(N→1)、回流；提交契约：全回流/择优回流/决策回流；条件：资格/审批/最大并行/成本池 |

补充约定：

- **海口 = 唯一面向用户的收敛通道**：所有水流的最终产物只在海口合成一次，
  从结构上杜绝"多节点各答一次"。
- **自治调度 = 加工即判定**：作用域完成本职时在产物上声明"下一步去哪个作用域 /
  过哪条通道 / 收"，路由决策并入产出，不另做判定调用。
- **载荷即端口**：作用域声明它消费/产出什么（取代 read_fields/output_field 的裸键
  升级为作用域契约：形态 + schema + 归属），水流载荷按契约传递，旧产物不残留。

### 9.2 执行语义

- 水流从入口作用域开始；每一步：当前作用域加工（可能调工具/能力）→ 按产物
  `__next` 决定下一作用域或收敛；到海口 = 会话收尾。
- **并行/多协作者**：fan-out 派 N 股子水流（各带独立作用域、独立轨迹/成本，天然
  并行），fan-in 按提交契约汇流（全部回流=spawn、择优回流=推演、决策回流=fork_trial）。
- **跨作用域/委托/子代理**：水流必须过通道（渗透语义）进入目标作用域的入口，
  不能凭空到达任意节点——隔离 = "只能走受控通道"，不是静态墙。
- 护栏只剩兜底：步数/成本上限、作用域访问资格、审批，不再承担主成本控制
  （成本由自治"何时收"决定）。

### 9.3 图 = 投影（不限制执行）

- 节点/边/图仅是**事后投影**：把水流走过的作用域与通道画出来，用于展示、审计、
  解释、择优观察。执行不依赖静态拓扑。
- "可达性"由作用域定义 + 通道条件动态判定，不需要预画边。
- 一张根图内可有嵌套子区域（agent/作用域的组织结构投影），是子结构而非多张平级图。

### 9.4 择优与进化的对象 = 组织资产

- 择优对象从"图拓扑"改为**组织决策**：该不该委托 / 并行几股 / 派给哪些作用域 /
  汇流契约 / 通道条件。
- **轨迹（scope×channel 序列）+ 结果（成败/成本）→ 组织档案**；跨会话对照单元 =
  组织模式（scope×channel 组合），非图。
- **受控进化**（仍是 InKling 与 dsh 的分界）：
  - 加/改/下架作用域定义（persona/model/能力/契约）；
  - 加/改/封通道条件；委托策略先验更新；常胜模式**短路化**（沉淀为直达作用域/直河）；
  - 全部走审批 + 补丁链 + Guard；孵化（统计结晶）同一条受控通道；
  - 初始目录 = 最小（main 作用域 + 基础能力），不预设 planner/reviewer 等角色链。

### 9.5 agent / 协作者 / 子代理归位

| 概念 | 归位 |
|---|---|
| agent 定义 | 作用域资产（实体目录数据真源，persona/model/role 不变） |
| 协作者/子代理调用 | 委托通道 → 目标作用域入口 → 子水流（独立 scope/轨迹/成本）→ 汇流 |
| 递归/嵌套 | 子水流内再 fan-out（委托更深作用域）；无上限但受护栏 |
| 临时协作者 | 动态创建 scope + spawn 通道 |
| 并行多协作者 | fan-out 多股子水流同时在不同作用域流动，主水流 fan-in 组织汇流 |

### 9.6 新旧语义映射

| 旧概念（§0–§八） | 新语义（§九） |
|---|---|
| 组装/重组装/按轮组装/t0 建图 | **废除**（无此动作） |
| 会话级骨架 / 图生命周期 | 水流（会话=一段水流；checkpoint 恢复状态与轨迹） |
| 分诊门（闲聊 vs 任务） | 入流时入口作用域选择（局部判定，非建图、非全局二分） |
| 图/节点/边（执行蓝图） | 投影；真实构件 = 作用域与通道 |
| agent 递归展开子图 | fan-out 委托进入目录作用域（子水流） |
| 池子/候选链/指纹缓存 | 作用域目录 + 通道条件资产 |
| planner/reviewer 预设角色 | 不作为初始组成；可选作用域按需委托，是否保留由组织择优决定 |
| 展开三通道（spawn/推演/fork_trial） | 通道提交契约（全回流/择优回流/决策回流） |
| 端口/read_fields 契约 | 作用域输入/输出契约 + 水流载荷 |
| 受审模板 / 槽位填充 / 结构发生器 | 组织先验（择优结晶的 scope×channel 模式） |
| 作用域 model 覆盖（P5-E） | 作用域属性天然生效（入 scope 即应用 persona/model） |
| 会话结构档案（P5-G） | 组织档案（scope×channel 模式统计） |
| 执行体"真分化" | 作用域定义分化（persona/契约/能力），按需委托 |
| 边界质量闸 | 作用域产物校验 + 海口合成校验（可选质量门作用域） |

### 9.7 对实现的影响

- **保留且仍有效**：受控通道/审批/补丁链/GuardedStorage/审计；作用域 model 覆盖
  seam；轨迹/事件流/checkpoint；guard/hygiene；fork_trial 等提交契约语义。
- **退役 / 待重新定位**：path_assembler / `_forward_search` / 池候选 / 指纹缓存
  图组装链路；per-session 骨架建图语义；per-session 图落库。
- **P5 队列修订**（对象从"组装/模板/发生器"改为"作用域与通道组织资产演化"，占位，
  后续细化）：
  - P5-α：作用域目录 + 通道条件数据面（受控注册，替代 §八 P5-I 的组装数据面）；
  - P5-β：组织档案 + 择优（轨迹 scope×channel 统计、委托/并行模式择优，替代 P5-G）；
  - P5-γ：受控演化接口（增/改/下架作用域、改通道条件、短路化、下架——替代模板类阶段）；
  - P5-δ：水流运行时（作用域装载、通道执行、海口合成、护栏）。

### 9.8 开放问题（后续继续收敛）

1. 海口"最终产物"的结构（纯文本 vs 结构化）与多轮续流（历史如何随水流携带）。
2. 作用域切换时载荷语义（什么数据带过去、什么留在原作用域）。
3. 通道资格/审批与安全模型的具体化。
4. 组织择优的信号定义与成本定价。
5. 工具（bash/web/fs）归入作用域能力 vs 独立工具目录。
6. 实验（experiment_river v2）沉淀的参数结论（reasoning_effort、结构化输出健壮契约）。

### 9.9 落地状态

| 项 | 状态 | 备注 |
|---|---|---|
| 9.1–9.5 最小原语模型 | 设计定稿（2026-09-09） | 当前主线 |
| 9.6 新旧映射 | 设计定稿 | §0–§八 冲突处以 §九 为准 |
| 9.7 实现影响与 P5 修订 | 待排期 | P5-α~δ 占位，后续细化 |
| 9.8 开放问题 | 未决 | 逐项收敛后回填 |
