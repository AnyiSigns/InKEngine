{
  "name": "@ink-ts/frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "L5 React/TS 纯渲染层：只渲染数据（ui_spec/事件流）+ 触发补丁；组件白名单注册制",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  }
}
