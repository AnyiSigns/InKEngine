{
  "environments": [
    {
      "name": "inkling.local",
      "runtime": "local",
      "tools": [],
      "install_cmds": [],
      "version": "0.1.0",
      "meta": {
        "purpose": "默认形态：本机运行时（白名单安装命令经 ProcessSandbox 执行）",
        "versioned_by_patch_chain": true,
        "env_vars": {
          "INKLING_LOCAL_READY": "1"
        },
        "note": "环境声明 = 数据：随 PatchKind.ENVIRONMENT 补丁版本化，回退 = 声明回退 + 实例重建；声明变更（含版本约束）触发旧实例销毁重建"
      }
    },
    {
      "name": "inkling.web_bridge",
      "runtime": "web_bridge",
      "tools": [],
      "install_cmds": [],
      "version": "0.1.0",
      "meta": {
        "purpose": "浏览器端形态（iframe 桥）：无安装/运行概念，ensure 恒就绪",
        "versioned_by_patch_chain": true,
        "note": "浏览器天然隔离（sandbox 属性 + postMessage 协议）；后端子进程运行显式不支持"
      }
    },
    {
      "name": "inkling.container",
      "runtime": "container",
      "tools": [],
      "install_cmds": [],
      "version": "0.1.0",
      "meta": {
        "purpose": "容器形态：镜像描述 = 数据，可销毁重建；M3 出厂落地 container_provider（ensure 幂等/run/destroy 幂等）",
        "versioned_by_patch_chain": true,
        "image": {
          "name": "inkling/base:0.1.0",
          "build_context": null,
          "note": "镜像名 = 数据（补丁链版本化）；build_context 为 null 时用安装命令经构建管线产镜像"
        },
        "note": "桌面无 Docker 不阻塞（local 为默认形态）；安装经沙箱白名单 + 审计链"
      }
    }
  ]
}
