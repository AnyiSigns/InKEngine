{
  "tiers": ["router", "tool", "main", "audit"],
  "default_tier": "main",
  "model_config": {
    "router_config": {
      "purpose": "路由/蒸馏等轻量决策（省成本）；配置缺失回落 main_config",
      "requires_user_config": true
    },
    "tool_config": {
      "purpose": "工具参数生成（中等挡位）",
      "requires_user_config": true
    },
    "main_config": {
      "purpose": "内容生成主挡位（默认回落目标）",
      "requires_user_config": true
    },
    "audit_config": {
      "purpose": "评审/审计挡位（质量把关）",
      "requires_user_config": true
    }
  },
  "fallback": {
    "unknown_tier_falls_to": "main",
    "missing_tier_config_falls_to": "main_config",
    "note": "与引擎 tiers 语义对齐：tier_key 未知回落 main；resolve_tier_config 缺挡位配置回落 main_config；实际连接配置（base_url/模型/密钥引用）由宿主注入，归宿主职责"
  }
}
